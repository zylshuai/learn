Day 2 应用能力测试
预计用时： 10-15 分钟

1. 闭包状态隔离 (代码分析)
阅读以下代码，并写出最终的控制台输出结果。

思考点： double 和 triple 是否会互相影响？为什么？

2. 函数柯里化与组合 (实践应用)
假设你有一个处理字符串的函数 processText(text, prefix, suffix)。

你的任务是： 使用函数柯里化的思想，创建一个新的、更专用的函数 createStandardGreeter。这个新函数应该只接收一个 name 参数，并返回一个形如 "Hello, [name] !" 的字符串。

请写出 createStandardGreeter 的实现代码。

3. 自定义迭代器 (动手编码)
下面是一个 Team 对象，它有一个成员列表 members。

你的任务是： 为 team 对象实现 [Symbol.iterator] 方法，使得我们可以直接使用 for...of 循环来遍历它的所有 members。

预期的效果：

4. 箭头函数陷阱 (Bug 排查)
下面的代码意图是创建一个 Logger 对象，它的 log 方法可以接收任意数量的参数，并将它们打印出来。但是，当前代码运行会报错或行为不符合预期。

你的任务是：

指出代码中 arguments 和 this 分别存在什么问题。
修改 log 方法的实现，使其能够正确工作。